

application.finalName=configurations-test
## base path to migration script
property.mongobee.changelog.path=inspectit.rocks.marketplace.configuration.service.migration.mongobee
property.mongobee.changelog.isEnabled=true

# set server port
server.port=8881

# Application Properties
marketplace.application.name=${group}.\${application.finalName}
marketplace.application.version=${version}

# Database
spring.data.mongodb.uri=mongodb://localhost/
spring.data.mongodb.database=marketplace-test