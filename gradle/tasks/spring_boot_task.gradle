/**
 * Add application Task to set Spring Profile.
 *
 * @author Nikita Kolytschew
 * @version %I%, %G%
 * @since 1.0.1-SNAPSHOT
 */


// bootRun will rely on the -P profile argument,
// defaulting to the "dev" profile if no -P arguments are provided
bootRun {
	// pass command line options from gradle to bootRun
	// usage: gradlew bootRun "-Dspring.profiles.active=local,protractor"
	println 'usage: gradlew bootRun "-Dspring.profiles.active=local,protractor"'
	systemProperty "spring.profiles.active", activeProfile
}

